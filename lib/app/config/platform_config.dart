class PlatformConfig {
  // Platform platform specific settings

  PlatformConfig._();

  factory PlatformConfig.web() {
    return PlatformConfig._();
    // Web platform specific settings
  }

  factory PlatformConfig.ios() {
    return PlatformConfig._();
    // iOS platform specific settings
  }

  factory PlatformConfig.android() {
    return PlatformConfig._();
    // Android platform specific settings
  }

  factory PlatformConfig.macos() {
    return PlatformConfig._();
    // macOS platform specific settings
  }

  factory PlatformConfig.linux() {
    return PlatformConfig._();
    // Linux platform specific settings
  }

  factory PlatformConfig.windows() {
    return PlatformConfig._();
    // Windows platform specific settings
  }

//Add other platforms functions
}
